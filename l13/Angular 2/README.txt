These files aren't designed to run on their own.  There are a lot more files you'll need to actually get the example running (the standard QuickStart build environment inclues over 12,000 files!)

These are the only files out of the 12,000+ I modified to run the example.